apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service
  namespace: fraud
  labels:
    app: fraud-detection
spec:
  selector:
    app: fraud-detection
  ports:
  - name: http
    port: 80
    targetPort: 8000
